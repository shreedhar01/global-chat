# 1. Ignore Git files
.git
.gitignore

# 2. Ignore ALL node_modules (Root, Frontend, and Backend)
node_modules
frontend/node_modules
backend/node_modules

# 3. Ignore Build Artifacts
frontend/dist
backend/dist

# 4. Ignore Environment Variables (Security)
.env
frontend/.env
backend/.env

# 5. Ignore Docker/Compose files (Optional, but clean)
docker-compose.yml
Dockerfile
frontend/Dockerfile
backend/Dockerfile